# SearXNGのリミッター設定。
# Render上での個人〜小規模利用を想定し、緩めのbot対策のみ有効にする。

[real_ip]
x_for = 1
ipv4_prefix = 32
ipv6_prefix = 48

[botdetection.ip_limit]
filter_link_local = true
link_token = false

[botdetection.ip_lists]
block_ip = []
pass_ip = []
